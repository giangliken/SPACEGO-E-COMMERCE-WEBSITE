
@{
    ViewData["Title"] = "Reset mật khẩu";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <h3 class="text-danger mb-3">⚠️ Xác nhận reset mật khẩu</h3>

    <p>Bạn có chắc muốn reset mật khẩu cho tài khoản <strong>@Model.Email</strong> không?</p>
    <p class="text-info">Mật khẩu mới sẽ là: <code>Abc@123</code></p>

    <form asp-action="ResetPassword" method="post">
        <input type="hidden" name="id" value="@Model.UserId" />
        <button type="submit" class="btn btn-danger">Xác nhận reset</button>
        <a asp-action="Index" class="btn btn-secondary">Huỷ</a>
    </form>
</div>
@Html.ValidationSummary()
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

